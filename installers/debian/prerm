#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        if command -v secret-stripper >/dev/null 2>&1; then
            secret-stripper uninstall >/dev/null 2>&1 || true
        fi
        ;;
esac
